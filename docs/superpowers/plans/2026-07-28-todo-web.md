# Tickly 阶段 4 Todo Web Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有认证壳层上交付支持快速新增、筛选排序、cursor 分页、编辑、完成回滚、删除确认和账号时区的响应式 Todo 工作区。

**Architecture:** 任务代码内聚在 `apps/web/src/features/tasks`，由 `useTaskWorkspace` 统一管理服务端列表、请求 generation、分页和 mutation 状态，展示组件只接收类型化 props。请求继续复用认证层的 `apiFetch`，通用错误解析下沉到 `apps/web/src/lib/api-error.ts`；任务和 token 均不进入浏览器持久化或全局 Store。

**Tech Stack:** React 19、TypeScript 6、Vite 8、Vitest 4、Testing Library、Tailwind CSS 4、Base UI 1.6、`@internationalized/date`

---

## 文件结构

新增文件：

- `apps/web/src/lib/api-error.ts`：安全错误 envelope、`ApiError`、错误响应解析和用户消息选择。
- `apps/web/src/lib/api-error.test.ts`：错误解析的稳定行为测试。
- `apps/web/src/features/tasks/task-api.ts`：任务 DTO、query 和四个实际使用的 HTTP operation。
- `apps/web/src/features/tasks/task-api.test.ts`：方法、路径、查询、请求体、错误和 `204` 契约测试。
- `apps/web/src/features/tasks/task-time.ts`：账号 IANA 时区、UTC 和 `datetime-local` 的集中转换。
- `apps/web/src/features/tasks/task-time.test.ts`：普通时区、跨日、DST 不存在及重复时间测试。
- `apps/web/src/features/tasks/use-task-workspace.ts`：列表 generation、分页、选择和 CRUD mutation 状态。
- `apps/web/src/features/tasks/use-task-workspace.test.tsx`：请求竞争、分页、刷新和乐观回滚测试。
- `apps/web/src/features/tasks/quick-create-form.tsx`：只输入标题的快速新增表单。
- `apps/web/src/features/tasks/task-toolbar.tsx`：状态筛选、排序字段和顺序。
- `apps/web/src/features/tasks/task-list.tsx`：骨架、错误、空状态、任务列表和加载更多。
- `apps/web/src/features/tasks/task-row.tsx`：任务摘要、完成复选框和编辑入口。
- `apps/web/src/features/tasks/task-editor-panel.tsx`：响应式编辑对话框和实际 PATCH 字段计算。
- `apps/web/src/features/tasks/delete-task-dialog.tsx`：显示标题的二次删除确认。
- `apps/web/src/features/tasks/todo-workspace.tsx`：组合工作区、局部错误和脏表单切换保护。
- `apps/web/src/features/tasks/todo-workspace.test.tsx`：真实用户行为、键盘、错误和壳层验收。

修改文件：

- `apps/web/src/features/auth/auth-api.ts`：复用公共错误解析，保留 token、refresh 和重试语义。
- `apps/web/src/features/auth/auth-context.test.tsx`：继续保护 refresh 和内存 token 行为。
- `apps/web/src/features/auth/authenticated-shell.tsx`：把账号信息和退出动作传给任务工作区。
- `apps/web/src/features/auth/login-form.test.tsx`：把认证成功断言校准为 Todo 工作区。
- `apps/web/src/index.css`：工作区、任务行、右侧面板和手机抽屉样式。
- `apps/web/package.json`、`pnpm-lock.yaml`：只增加 `@internationalized/date`。
- `README.md`、`AGENTS.md`、`docs/roadmaps/2026-07-26-tickly-zero-to-one.md`：阶段完成后校准真实能力和下一阶段。

不修改 `apps/api` 生产代码、schema、migration 或 Python 锁文件。

### Task 1: 提取安全 API 错误边界

**Files:**
- Create: `apps/web/src/lib/api-error.ts`
- Create: `apps/web/src/lib/api-error.test.ts`
- Modify: `apps/web/src/features/auth/auth-api.ts`
- Test: `apps/web/src/features/auth/auth-context.test.tsx`

- [ ] **Step 1: 写公共错误解析的失败测试**

```ts
import { describe, expect, it } from "vitest"

import { ApiError, errorCode, responseError, safeErrorMessage } from "./api-error"

function jsonResponse(body: unknown, status: number) {
  return new Response(JSON.stringify(body), {
    status,
    headers: { "Content-Type": "application/json" },
  })
}

describe("API 错误边界", () => {
  it("读取统一错误 envelope 且不丢失原响应", async () => {
    const response = jsonResponse(
      { error: { code: "task_not_found", message: "任务不存在" } },
      404,
    )

    expect(await errorCode(response)).toBe("task_not_found")
    await expect(responseError(response)).resolves.toMatchObject({
      status: 404,
      code: "task_not_found",
      message: "任务不存在",
    })
  })

  it("非 JSON 响应和未知异常只显示安全兜底", async () => {
    const error = await responseError(new Response("secret", { status: 500 }))

    expect(error).toEqual(
      expect.objectContaining({ status: 500, code: "request_failed" }),
    )
    expect(safeErrorMessage(error, "操作失败")).toBe("请求失败")
    expect(safeErrorMessage(new Error("secret"), "操作失败")).toBe("操作失败")
    expect(error).toBeInstanceOf(ApiError)
  })
})
```

- [ ] **Step 2: 运行测试并确认因模块不存在而失败**

Run: `mise exec -- pnpm --filter @tickly/web test -- src/lib/api-error.test.ts`

Expected: FAIL，提示无法解析 `./api-error`。

- [ ] **Step 3: 实现公共错误模块并切换认证请求**

```ts
// apps/web/src/lib/api-error.ts
type ErrorEnvelope = {
  error?: {
    code?: string
    message?: string
  }
}

export class ApiError extends Error {
  readonly status: number
  readonly code: string

  constructor(status: number, code: string, message: string) {
    super(message)
    this.name = "ApiError"
    this.status = status
    this.code = code
  }
}

export async function errorCode(response: Response): Promise<string | undefined> {
  try {
    const payload = (await response.clone().json()) as ErrorEnvelope
    return payload.error?.code
  } catch {
    return undefined
  }
}

export async function responseError(response: Response): Promise<ApiError> {
  try {
    const payload = (await response.clone().json()) as ErrorEnvelope
    return new ApiError(
      response.status,
      payload.error?.code ?? "request_failed",
      payload.error?.message ?? "请求失败",
    )
  } catch {
    return new ApiError(response.status, "request_failed", "请求失败")
  }
}

export function safeErrorMessage(error: unknown, fallback: string): string {
  return error instanceof ApiError ? error.message : fallback
}
```

在 `auth-api.ts` 顶部导入公共符号：

```ts
import { errorCode, responseError } from "@/lib/api-error"
```

删除 `auth-api.ts` 中本地的 `ErrorEnvelope`、`ApiError`、`errorCode` 和 `responseError`，其余 `apiFetch`、单飞 refresh、内存 token 和认证失败回调保持原样。

- [ ] **Step 4: 运行公共错误与认证回归测试**

Run: `mise exec -- pnpm --filter @tickly/web test -- src/lib/api-error.test.ts src/features/auth/auth-context.test.tsx`

Expected: 两个测试文件全部 PASS；并发认证失败仍只发起一个 refresh。

- [ ] **Step 5: 提交公共错误边界**

```powershell
git add -- apps/web/src/lib/api-error.ts apps/web/src/lib/api-error.test.ts apps/web/src/features/auth/auth-api.ts
git commit -m "refactor(web): 复用安全 API 错误边界"
```

### Task 2: 建立类型化任务 API 客户端

**Files:**
- Create: `apps/web/src/features/tasks/task-api.ts`
- Create: `apps/web/src/features/tasks/task-api.test.ts`

- [ ] **Step 1: 写四个实际 operation 的失败测试**

测试用 `vi.mock("@/features/auth/auth-api")` 注入 `apiFetch`，覆盖以下精确断言：

```ts
const query: TaskListQuery = {
  status: "active",
  sort: "priority",
  order: "asc",
  limit: 50,
  cursor: "next/+==",
}

await listTasks(query, controller.signal)
expect(api.apiFetch).toHaveBeenCalledWith(
  "/api/v1/tasks?status=active&sort=priority&order=asc&limit=50&cursor=next%2F%2B%3D%3D",
  { signal: controller.signal },
)

await createTask({ title: "阶段 4" })
expect(api.apiFetch).toHaveBeenCalledWith("/api/v1/tasks", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ title: "阶段 4" }),
})

await updateTask("task/id", { notes: null, due_at: null })
expect(api.apiFetch).toHaveBeenCalledWith("/api/v1/tasks/task%2Fid", {
  method: "PATCH",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ notes: null, due_at: null }),
})

api.apiFetch.mockResolvedValueOnce(new Response(null, { status: 204 }))
await expect(deleteTask("task-id")).resolves.toBeUndefined()
```

另加两个用例：非成功响应抛出 `ApiError`；DELETE `204` 不调用 `response.json()`。

- [ ] **Step 2: 运行测试并确认因客户端不存在而失败**

Run: `mise exec -- pnpm --filter @tickly/web test -- src/features/tasks/task-api.test.ts`

Expected: FAIL，提示无法解析 `./task-api`。

- [ ] **Step 3: 实现 DTO、query 和请求函数**

```ts
import { apiFetch } from "@/features/auth/auth-api"
import { responseError } from "@/lib/api-error"

export type TaskPriority = "none" | "low" | "medium" | "high"
export type TaskStatus = "all" | "active" | "completed"
export type TaskSort = "created_at" | "due_at" | "priority"
export type SortOrder = "asc" | "desc"

export type Task = {
  id: string
  title: string
  notes: string | null
  is_completed: boolean
  priority: TaskPriority
  due_at: string | null
  completed_at: string | null
  created_at: string
  updated_at: string
}

export type TaskPage = { items: Task[]; next_cursor: string | null }
export type TaskListQuery = {
  status: TaskStatus
  sort: TaskSort
  order: SortOrder
  limit: number
  cursor?: string
}
export type TaskCreateInput = {
  title: string
  notes?: string | null
  priority?: TaskPriority
  due_at?: string | null
}
export type TaskUpdateInput = Partial<
  Pick<Task, "title" | "notes" | "priority" | "due_at" | "is_completed">
>

export const DEFAULT_TASK_QUERY = {
  status: "all",
  sort: "created_at",
  order: "desc",
  limit: 50,
} satisfies TaskListQuery

async function readJson<T>(response: Response): Promise<T> {
  if (!response.ok) throw await responseError(response)
  return (await response.json()) as T
}

export async function listTasks(
  query: TaskListQuery,
  signal?: AbortSignal,
): Promise<TaskPage> {
  const params = new URLSearchParams({
    status: query.status,
    sort: query.sort,
    order: query.order,
    limit: String(query.limit),
  })
  if (query.cursor !== undefined) params.set("cursor", query.cursor)
  return readJson<TaskPage>(
    await apiFetch(`/api/v1/tasks?${params.toString()}`, { signal }),
  )
}

export async function createTask(input: TaskCreateInput): Promise<Task> {
  return readJson<Task>(
    await apiFetch("/api/v1/tasks", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(input),
    }),
  )
}

export async function updateTask(
  taskId: string,
  input: TaskUpdateInput,
): Promise<Task> {
  return readJson<Task>(
    await apiFetch(`/api/v1/tasks/${encodeURIComponent(taskId)}`, {
      method: "PATCH",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(input),
    }),
  )
}

export async function deleteTask(taskId: string): Promise<void> {
  const response = await apiFetch(`/api/v1/tasks/${encodeURIComponent(taskId)}`, {
    method: "DELETE",
  })
  if (!response.ok) throw await responseError(response)
}
```

- [ ] **Step 4: 运行客户端测试**

Run: `mise exec -- pnpm --filter @tickly/web test -- src/features/tasks/task-api.test.ts`

Expected: PASS，四个 operation 的方法、路径、body、query、错误和 `204` 契约均符合阶段 3 API。

- [ ] **Step 5: 提交任务客户端**

```powershell
git add -- apps/web/src/features/tasks/task-api.ts apps/web/src/features/tasks/task-api.test.ts
git commit -m "feat(web): 增加 Todo API 客户端"
```

### Task 3: 实现账号时区转换

**Files:**
- Modify: `apps/web/package.json`
- Modify: `pnpm-lock.yaml`
- Create: `apps/web/src/features/tasks/task-time.ts`
- Create: `apps/web/src/features/tasks/task-time.test.ts`

- [ ] **Step 1: 增加唯一新依赖**

Run: `mise exec -- pnpm --filter @tickly/web add @internationalized/date`

Expected: `apps/web/package.json` 出现 `@internationalized/date`，根 `pnpm-lock.yaml` 更新，不产生应用级锁文件。

- [ ] **Step 2: 写 UTC、跨日和 DST 失败测试**

```ts
import { describe, expect, it } from "vitest"

import {
  TaskTimeError,
  formatDueLabel,
  toDateTimeLocalValue,
  toUtcDueAt,
} from "./task-time"

describe("账号时区截止时间", () => {
  it("按账号时区在 UTC 与 datetime-local 间转换", () => {
    expect(toDateTimeLocalValue("2026-07-30T10:00:00Z", "Asia/Shanghai"))
      .toBe("2026-07-30T18:00")
    expect(toUtcDueAt("2026-07-30T18:00", "Asia/Shanghai"))
      .toBe("2026-07-30T10:00:00.000Z")
    expect(toUtcDueAt("", "Asia/Shanghai")).toBeNull()
  })

  it("拒绝跳时且为重复时间选择较早时刻", () => {
    expect(() =>
      toUtcDueAt("2026-03-08T02:30", "America/Los_Angeles"),
    ).toThrow(TaskTimeError)
    expect(toUtcDueAt("2026-11-01T01:30", "America/Los_Angeles"))
      .toBe("2026-11-01T08:30:00.000Z")
  })

  it("以账号时区生成明确的今天和逾期文本", () => {
    const now = new Date("2026-07-30T10:30:00Z")
    expect(formatDueLabel("2026-07-30T12:00:00Z", "Asia/Shanghai", now))
      .toContain("今天")
    expect(formatDueLabel("2026-07-30T09:00:00Z", "Asia/Shanghai", now))
      .toContain("已逾期")
  })
})
```

- [ ] **Step 3: 运行测试并确认因时间模块不存在而失败**

Run: `mise exec -- pnpm --filter @tickly/web test -- src/features/tasks/task-time.test.ts`

Expected: FAIL，提示无法解析 `./task-time`。

- [ ] **Step 4: 实现分钟精度、DST 消歧和展示标签**

```ts
import { parseAbsolute, parseDateTime, toZoned } from "@internationalized/date"

export class TaskTimeError extends Error {
  constructor(message: string) {
    super(message)
    this.name = "TaskTimeError"
  }
}

function pad(value: number): string {
  return String(value).padStart(2, "0")
}

function wallValue(value: ReturnType<typeof parseAbsolute>): string {
  return `${value.year}-${pad(value.month)}-${pad(value.day)}T${pad(value.hour)}:${pad(value.minute)}`
}

export function toDateTimeLocalValue(utcIso: string, timeZone: string): string {
  return wallValue(parseAbsolute(utcIso, timeZone))
}

export function toUtcDueAt(value: string, timeZone: string): string | null {
  if (value === "") return null
  try {
    const wallTime = parseDateTime(value)
    const earlier = toZoned(wallTime, timeZone, "earlier")
    const later = toZoned(wallTime, timeZone, "later")
    // 跳时的不存在时间无法往返为原墙上时间；重复时间则两个候选均可往返。
    if (wallValue(earlier) !== value || wallValue(later) !== value) {
      throw new TaskTimeError("该截止时间因夏令时切换而不存在")
    }
    return earlier.toAbsoluteString()
  } catch (error) {
    if (error instanceof TaskTimeError) throw error
    throw new TaskTimeError("截止时间格式无效")
  }
}

function dateKey(utcIso: string, timeZone: string): string {
  const value = parseAbsolute(utcIso, timeZone)
  return `${value.year}-${pad(value.month)}-${pad(value.day)}`
}

export function formatDueLabel(
  dueAt: string,
  timeZone: string,
  now = new Date(),
): string {
  const formatter = new Intl.DateTimeFormat("zh-CN", {
    timeZone,
    month: "short",
    day: "numeric",
    hour: "2-digit",
    minute: "2-digit",
    hour12: false,
  })
  const prefix =
    Date.parse(dueAt) < now.getTime()
      ? "已逾期"
      : dateKey(dueAt, timeZone) === dateKey(now.toISOString(), timeZone)
        ? "今天"
        : "截止"
  return `${prefix} · ${formatter.format(new Date(dueAt))}`
}
```

- [ ] **Step 5: 运行时间测试并提交**

Run: `mise exec -- pnpm --filter @tickly/web test -- src/features/tasks/task-time.test.ts`

Expected: PASS；不存在时间被拒绝，重复时间固定为较早 UTC instant。

```powershell
git add -- apps/web/package.json pnpm-lock.yaml apps/web/src/features/tasks/task-time.ts apps/web/src/features/tasks/task-time.test.ts
git commit -m "feat(web): 支持账号时区截止时间"
```

### Task 4: 实现列表、筛选和 cursor 分页状态

**Files:**
- Create: `apps/web/src/features/tasks/use-task-workspace.ts`
- Create: `apps/web/src/features/tasks/use-task-workspace.test.tsx`

- [ ] **Step 1: 写首次读取、旧响应隔离和分页去重测试**

使用 `renderHook` mock `task-api.ts`，建立两个可手动 resolve 的 Promise。测试必须证明：

```ts
function deferred<T>() {
  let resolve!: (value: T) => void
  let reject!: (reason?: unknown) => void
  const promise = new Promise<T>((onResolve, onReject) => {
    resolve = onResolve
    reject = onReject
  })
  return { promise, resolve, reject }
}

const { result } = renderHook(() => useTaskWorkspace())
await waitFor(() => expect(tasks.listTasks).toHaveBeenCalledTimes(1))

act(() => result.current.actions.setStatus("active"))
await waitFor(() => expect(tasks.listTasks).toHaveBeenCalledTimes(2))

second.resolve({ items: [activeTask], next_cursor: "cursor-2" })
await waitFor(() => expect(result.current.state.items).toEqual([activeTask]))
first.resolve({ items: [staleTask], next_cursor: null })
expect(result.current.state.items).toEqual([activeTask])

await act(() => result.current.actions.loadMore())
expect(tasks.listTasks).toHaveBeenLastCalledWith(
  expect.objectContaining({ status: "active", cursor: "cursor-2" }),
  expect.any(AbortSignal),
)
expect(result.current.state.items.map((task) => task.id)).toEqual([
  activeTask.id,
  nextTask.id,
])
```

再覆盖首次失败显示 `error`、`retry()` 使用相同 query，以及加载更多失败时保留 items 和 cursor。

- [ ] **Step 2: 运行测试并确认 hook 尚不存在**

Run: `mise exec -- pnpm --filter @tickly/web test -- src/features/tasks/use-task-workspace.test.tsx`

Expected: FAIL，提示无法解析 `./use-task-workspace`。

- [ ] **Step 3: 实现公开状态契约和请求 generation**

公开契约固定为：

```ts
export type TaskWorkspaceState = {
  query: Omit<TaskListQuery, "cursor">
  items: Task[]
  nextCursor: string | null
  initialLoading: boolean
  loadingMore: boolean
  error: string | null
  selectedTaskId: string | null
}

export type TaskWorkspaceActions = {
  setStatus(status: TaskStatus): void
  setSort(sort: TaskSort): void
  setOrder(order: SortOrder): void
  retry(): Promise<void>
  loadMore(): Promise<void>
  selectTask(taskId: string | null): void
}
```

实现 `loadFirstPage(targetQuery)` 时必须按以下顺序：递增 `generationRef`、abort 旧 controller、清空旧 items/cursor/error、请求第一页、只让相同 generation 写入结果。`useEffect` 监听 `query` 并在 cleanup abort；AbortError 不进入界面错误。

`loadMore()` 捕获调用时的 generation 和 cursor，成功后使用以下函数按 ID 追加：

```ts
function appendUniqueTasks(current: Task[], incoming: Task[]): Task[] {
  const known = new Set(current.map((task) => task.id))
  return current.concat(incoming.filter((task) => !known.has(task.id)))
}
```

query setter 只更新真实变化的字段；query 变化后 effect 触发新第一页。`selectedTaskId` 不在新结果中时清空。

- [ ] **Step 4: 运行读取与分页测试**

Run: `mise exec -- pnpm --filter @tickly/web test -- src/features/tasks/use-task-workspace.test.tsx`

Expected: PASS；旧 generation 不覆盖新筛选，加载更多失败不破坏已有页。

- [ ] **Step 5: 提交读取状态**

```powershell
git add -- apps/web/src/features/tasks/use-task-workspace.ts apps/web/src/features/tasks/use-task-workspace.test.tsx
git commit -m "feat(web): 管理 Todo 列表与分页状态"
```

### Task 5: 完成 CRUD mutation 和乐观回滚

**Files:**
- Modify: `apps/web/src/features/tasks/use-task-workspace.ts`
- Modify: `apps/web/src/features/tasks/use-task-workspace.test.tsx`

- [ ] **Step 1: 写 mutation 失败测试**

新增用例覆盖：

```ts
await act(async () => {
  await result.current.actions.create("  新任务  ")
})
expect(tasks.createTask).toHaveBeenCalledWith({ title: "新任务" })
expect(tasks.listTasks).toHaveBeenCalledTimes(2)

const failedCompletion = deferred<Task>()
tasks.updateTask.mockReturnValueOnce(failedCompletion.promise)
let completion!: Promise<void>
act(() => {
  completion = result.current.actions.setCompleted(activeTask, true)
})
expect(result.current.state.items).not.toContainEqual(activeTask)
failedCompletion.reject(new Error("网络中断"))
await act(async () => {
  await expect(completion).rejects.toThrow()
})
expect(result.current.state.items[originalIndex]).toEqual(activeTask)

const crossGenerationFailure = deferred<Task>()
tasks.updateTask.mockReturnValueOnce(crossGenerationFailure.promise)
let crossedCompletion!: Promise<void>
act(() => {
  crossedCompletion = result.current.actions.setCompleted(activeTask, true)
})
act(() => result.current.actions.setStatus("completed"))
crossGenerationFailure.reject(new Error("网络中断"))
await act(async () => {
  await expect(crossedCompletion).rejects.toThrow()
})
expect(result.current.state.items).not.toContainEqual(activeTask)
expect(tasks.listTasks).toHaveBeenCalledWith(
  expect.objectContaining({ status: "completed" }),
  expect.any(AbortSignal),
)
```

另测同一任务完成请求期间 ID 位于 `completingTaskIds`；保存成功关闭选择并刷新；删除成功从当前 items 移除；失败不关闭选择。

- [ ] **Step 2: 运行新增测试并确认 mutation 尚未实现**

Run: `mise exec -- pnpm --filter @tickly/web test -- src/features/tasks/use-task-workspace.test.tsx`

Expected: FAIL，失败点位于 create/save/setCompleted/remove 的行为断言。

- [ ] **Step 3: 实现 mutation 不变量**

先把状态和动作契约扩展为：

```ts
export type TaskMutationState = {
  creating: boolean
  saving: boolean
  deleting: boolean
  completingTaskIds: ReadonlySet<string>
  completionError: string | null
}

export type TaskMutationActions = {
  create(title: string): Promise<void>
  save(taskId: string, patch: TaskUpdateInput): Promise<void>
  setCompleted(task: Task, completed: boolean): Promise<void>
  remove(taskId: string): Promise<void>
}
```

`TaskWorkspaceState` 合并 `TaskMutationState`，`TaskWorkspaceActions` 合并 `TaskMutationActions`，初始 mutation flag 全为 false、集合为空、错误为 null。

实现规则必须与以下伪状态转换一一对应，不增加后台缓存：

```ts
async function create(title: string) {
  const normalizedTitle = title.trim()
  if (normalizedTitle === "" || creatingRef.current) return
  creatingRef.current = true
  setState((current) => ({ ...current, creating: true }))
  try {
    await createTask({ title: normalizedTitle })
    await loadFirstPage(queryRef.current)
  } finally {
    creatingRef.current = false
    setState((current) => ({ ...current, creating: false }))
  }
}
```

`save()` 设置 `saving`，调用一次 `updateTask(taskId, patch)`，成功后清空选择并刷新当前 query；失败保持选择并重新抛出安全错误。

`remove()` 设置 `deleting`，DELETE 成功后按 ID 移除、清空选择；失败保留 items 和选择并重新抛出。

`setCompleted()` 必须保存 `originalTask`、`originalIndex` 和 `originalGeneration`。先依据当前 status 更新或移除任务；成功时只在相同 generation 中用响应替换仍存在的任务。失败时：

```ts
if (generationRef.current === originalGeneration) {
  setState((current) => {
    const restored = current.items.filter((item) => item.id !== originalTask.id)
    restored.splice(Math.min(originalIndex, restored.length), 0, originalTask)
    return { ...current, items: restored, completionError: "完成状态更新失败" }
  })
} else {
  setState((current) => ({
    ...current,
    completionError: "完成状态更新失败",
  }))
  await loadFirstPage(queryRef.current)
}
```

`finally` 从 `completingTaskIds` 删除当前 ID。所有 mutation flag 都必须在 `finally` 复位；未知异常向组件抛出但不得把原始 message 写入界面状态。

- [ ] **Step 4: 运行 hook 全部测试**

Run: `mise exec -- pnpm --filter @tickly/web test -- src/features/tasks/use-task-workspace.test.tsx`

Expected: PASS；同 generation 精确恢复原索引，跨 generation 不注入旧快照并刷新新 query。

- [ ] **Step 5: 提交 mutation 状态**

```powershell
git add -- apps/web/src/features/tasks/use-task-workspace.ts apps/web/src/features/tasks/use-task-workspace.test.tsx
git commit -m "feat(web): 完成 Todo 乐观交互状态"
```

### Task 6: 实现编辑面板与删除确认

**Files:**
- Create: `apps/web/src/features/tasks/task-editor-panel.tsx`
- Create: `apps/web/src/features/tasks/delete-task-dialog.tsx`
- Create: `apps/web/src/features/tasks/task-editor-panel.test.tsx`

- [ ] **Step 1: 写实际 PATCH、DST 错误和关闭保护测试**

```ts
render(
  <TaskEditorPanel
    task={task}
    timeZone="Asia/Shanghai"
    saving={false}
    deleting={false}
    error={null}
    onDirtyChange={onDirtyChange}
    onSave={onSave}
    onDelete={onDelete}
    onClose={onClose}
  />,
)

await user.clear(screen.getByLabelText("备注"))
await user.selectOptions(screen.getByLabelText("优先级"), "high")
await user.click(screen.getByRole("button", { name: "保存" }))
expect(onSave).toHaveBeenCalledWith({ notes: null, priority: "high" })

await user.clear(screen.getByLabelText("截止时间"))
await user.type(screen.getByLabelText("截止时间"), "2026-03-08T02:30")
await user.click(screen.getByRole("button", { name: "保存" }))
expect(await screen.findByRole("alert")).toHaveTextContent("夏令时")
expect(onSave).not.toHaveBeenCalled()

vi.spyOn(window, "confirm").mockReturnValue(false)
await user.keyboard("{Escape}")
expect(window.confirm).toHaveBeenCalledWith("放弃未保存的修改？")
expect(onClose).not.toHaveBeenCalled()
```

删除测试打开独立确认框，断言标题可见、取消不删除、确认只调用一次 `onDelete`，失败时确认框关闭而父编辑面板仍存在。

- [ ] **Step 2: 运行组件测试并确认文件不存在**

Run: `mise exec -- pnpm --filter @tickly/web test -- src/features/tasks/task-editor-panel.test.tsx`

Expected: FAIL，提示无法解析 `./task-editor-panel`。

- [ ] **Step 3: 实现表单快照和最小 PATCH**

表单快照固定为：

```ts
type EditorValues = {
  title: string
  notes: string
  priority: TaskPriority
  dueAt: string
}

function initialValues(task: Task, timeZone: string): EditorValues {
  return {
    title: task.title,
    notes: task.notes ?? "",
    priority: task.priority,
    dueAt: task.due_at === null ? "" : toDateTimeLocalValue(task.due_at, timeZone),
  }
}

export function buildTaskPatch(
  task: Task,
  values: EditorValues,
  timeZone: string,
): TaskUpdateInput {
  const patch: TaskUpdateInput = {}
  const title = values.title.trim()
  const notes = values.notes === "" ? null : values.notes
  const originalDueAt =
    task.due_at === null ? "" : toDateTimeLocalValue(task.due_at, timeZone)
  if (title !== task.title) patch.title = title
  if (notes !== task.notes) patch.notes = notes
  if (values.priority !== task.priority) patch.priority = values.priority
  if (values.dueAt !== originalDueAt) {
    patch.due_at = toUtcDueAt(values.dueAt, timeZone)
  }
  return patch
}
```

使用 `Dialog` from `@base-ui/react/dialog` 组合 `Root/Portal/Backdrop/Viewport/Popup/Title/Description`。`Root` 受控为打开；`onOpenChange(false, details)` 遇到脏表单且用户拒绝时调用 `details.cancel()`，否则调用 `onClose()`。Popup 使用同一 DOM，CSS 在桌面定位右侧、手机定位底部。

标题 `required maxLength={200}`，备注 `maxLength={4000}`，优先级是四个固定 option，截止时间是 `datetime-local`、`step={60}`。无真实修改、标题 trim 后为空或正在保存时禁用保存。`TaskTimeError` 显示在截止字段旁；API 错误由父组件传入 `error` 并显示为 `role="alert"`。

- [ ] **Step 4: 实现删除确认的独立 Dialog**

`DeleteTaskDialog` 接收 `taskTitle/open/deleting/onOpenChange/onConfirm`。标题为“删除任务？”，Description 包含原任务标题；取消调用 `onOpenChange(false)`，确认按钮使用 destructive variant。确认失败时组件先关闭自身，再由编辑面板显示父级 error，避免留下两个叠加 modal。

- [ ] **Step 5: 运行面板测试并提交**

Run: `mise exec -- pnpm --filter @tickly/web test -- src/features/tasks/task-editor-panel.test.tsx`

Expected: PASS；Base UI 提供焦点陷阱、Escape、外部点击和关闭后的焦点恢复，业务层正确拦截脏表单。

```powershell
git add -- apps/web/src/features/tasks/task-editor-panel.tsx apps/web/src/features/tasks/delete-task-dialog.tsx apps/web/src/features/tasks/task-editor-panel.test.tsx
git commit -m "feat(web): 增加 Todo 编辑与删除确认"
```

### Task 7: 组装任务工作区和响应式界面

**Files:**
- Create: `apps/web/src/features/tasks/quick-create-form.tsx`
- Create: `apps/web/src/features/tasks/task-toolbar.tsx`
- Create: `apps/web/src/features/tasks/task-row.tsx`
- Create: `apps/web/src/features/tasks/task-list.tsx`
- Create: `apps/web/src/features/tasks/todo-workspace.tsx`
- Create: `apps/web/src/features/tasks/todo-workspace.test.tsx`
- Modify: `apps/web/src/features/auth/authenticated-shell.tsx`
- Modify: `apps/web/src/features/auth/login-form.test.tsx`
- Modify: `apps/web/src/index.css`

- [ ] **Step 1: 写工作区用户行为失败测试**

用真实 `useTaskWorkspace`、mock `task-api.ts` 渲染 `TodoWorkspace`。覆盖以下用户路径：

```ts
expect(await screen.findByRole("heading", { name: "今天要完成什么？" }))
  .toBeInTheDocument()
expect(screen.getByText("potato")).toBeInTheDocument()
expect(screen.getByText("Asia/Shanghai")).toBeInTheDocument()

await user.type(screen.getByLabelText("任务标题"), "阶段 4{Enter}")
expect(tasks.createTask).toHaveBeenCalledWith({ title: "阶段 4" })
expect(screen.getByLabelText("任务标题")).toBeDisabled()

await user.click(screen.getByRole("button", { name: "进行中" }))
expect(tasks.listTasks).toHaveBeenLastCalledWith(
  expect.objectContaining({ status: "active" }),
  expect.any(AbortSignal),
)

await user.click(screen.getByRole("checkbox", { name: "将阶段 4 标记为已完成" }))
expect(screen.queryByText("阶段 4")).not.toBeInTheDocument()
```

另测初次骨架、完整错误和重试；空筛选文案；加载更多末页与失败重试；任务主体打开面板而 checkbox 不打开；脏表单切换任务需要确认；创建失败保留标题；完成失败显示局部错误；用户名、时区和退出入口仍可见。

- [ ] **Step 2: 运行测试并确认展示组件不存在**

Run: `mise exec -- pnpm --filter @tickly/web test -- src/features/tasks/todo-workspace.test.tsx`

Expected: FAIL，提示无法解析 `./todo-workspace`。

- [ ] **Step 3: 实现快速新增、工具栏和列表展示**

组件 props 固定为：

```ts
type QuickCreateFormProps = {
  creating: boolean
  onCreate(title: string): Promise<void>
}

type TaskToolbarProps = {
  status: TaskStatus
  sort: TaskSort
  order: SortOrder
  disabled: boolean
  onStatusChange(value: TaskStatus): void
  onSortChange(value: TaskSort): void
  onOrderChange(value: SortOrder): void
}

type TaskListProps = {
  tasks: Task[]
  status: TaskStatus
  timeZone: string
  initialLoading: boolean
  loadingMore: boolean
  nextCursor: string | null
  error: string | null
  completionError: string | null
  completingTaskIds: ReadonlySet<string>
  onRetry(): Promise<void>
  onLoadMore(): Promise<void>
  onSelect(task: Task): void
  onCompletedChange(task: Task, completed: boolean): Promise<void>
}
```

`QuickCreateForm` 在同一个 `onSubmit` 中 trim、调用 `onCreate`、成功清空、失败保留并用 `safeErrorMessage(error, "任务创建失败")` 显示局部 alert。请求期间禁用输入和添加按钮。

`TaskToolbar` 用三个 `aria-pressed` 按钮切换全部/进行中/已完成，并用两个有 label 的原生 select 切换创建时间/截止时间/优先级及升序/降序。

`TaskList` 按状态选择空文案，首次失败显示“重新加载”，加载更多失败保留已有任务并显示“重试”。`TaskRow` 的 checkbox 可访问名称包含标题，任务主体是 button；优先级同时显示中文文字，截止时间调用 `formatDueLabel()`，完成项增加删除线但保留对比度。

首次加载、加载更多和 mutation 进度使用 `role="status" aria-live="polite"`；失败信息使用 `role="alert"`。checkbox 请求期间仅禁用对应任务，快速新增、筛选、排序和编辑 mutation 也只禁用各自相关控件。

- [ ] **Step 4: 组合工作区、编辑错误和脏表单切换**

`TodoWorkspace` 接收以下壳层 props：

```ts
type TodoWorkspaceProps = {
  username: string
  timeZone: string
  loggingOut: boolean
  onLogout(): Promise<void>
}
```

它调用一次 `useTaskWorkspace()`，根据 `selectedTaskId` 从 items 取当前任务；编辑保存和删除分别 catch 后用 `safeErrorMessage` 写入 `editorError`。`editorDirty` 为 true 时切换任务必须先执行 `window.confirm("放弃未保存的修改？")`。确认后先清理错误与 dirty，再调用 `selectTask(newId)`。

`AuthenticatedShell` 保留现有 `loggingOut` 和 `handleLogout`，认证后改为：

```tsx
return (
  <TodoWorkspace
    username={state.user.username}
    timeZone={state.user.timezone}
    loggingOut={loggingOut}
    onLogout={handleLogout}
  />
)
```

更新 `login-form.test.tsx` 的认证成功断言：mock 第一页任务响应，等待“今天要完成什么？”和账号时区出现，再点击退出；不再断言“认证已就绪”。

- [ ] **Step 5: 增加响应式样式并运行工作区测试**

在 `index.css` 新增 `.todo-page`、`.todo-shell`、`.todo-header`、`.todo-grid`、`.task-list`、`.task-row`、`.task-dialog-backdrop`、`.task-dialog-viewport`、`.task-editor-popup`。默认 Popup 固定底部、圆角顶部、最大高度 `88svh`；`min-width: 768px` 时改为右侧全高面板、宽度 `min(32rem, 42vw)`。所有可交互项最小触控高度 `44px`；`prefers-reduced-motion` 下移除 dialog 和 task-row 的 transition。

Run: `mise exec -- pnpm --filter @tickly/web test -- src/features/tasks/todo-workspace.test.tsx src/features/auth/login-form.test.tsx`

Expected: PASS；手机和桌面共用同一业务 DOM，语义、错误、键盘和壳层契约完整。

- [ ] **Step 6: 提交完整工作区**

```powershell
git add -- apps/web/src/features/tasks/quick-create-form.tsx apps/web/src/features/tasks/task-toolbar.tsx apps/web/src/features/tasks/task-row.tsx apps/web/src/features/tasks/task-list.tsx apps/web/src/features/tasks/todo-workspace.tsx apps/web/src/features/tasks/todo-workspace.test.tsx apps/web/src/features/auth/authenticated-shell.tsx apps/web/src/features/auth/login-form.test.tsx apps/web/src/index.css
git commit -m "feat(web): 交付响应式 Todo 工作区"
```

### Task 8: 校准文档并执行阶段验收

**Files:**
- Modify: `README.md`
- Modify: `AGENTS.md`
- Modify: `docs/roadmaps/2026-07-26-tickly-zero-to-one.md`

- [ ] **Step 1: 更新真实能力描述**

README 当前状态改为阶段 0–4 已完成：Web 已支持服务端 Todo CRUD、状态筛选、排序、cursor 加载更多、账号时区和响应式编辑；AI 仍未实现，账号仍通过 CLI 创建。

AGENTS 的 `apps/web` 当前状态改为已有认证与 Todo 工作区；不得把 AI、导出、离线或实时能力写成已实现。

路线图顶部状态改为阶段 0–4 已实现，末尾“下一份规格”改为阶段 5 自然语言任务草稿；阶段 4 验收条目保持不含数据导出。

- [ ] **Step 2: 运行 Web 全量测试**

Run: `mise exec -- pnpm test:web`

Expected: Vitest 所有测试文件 PASS，进程 exit code 0。

- [ ] **Step 3: 运行 Web 静态检查与构建**

Run: `mise exec -- pnpm lint`

Expected: ESLint exit code 0，无 error。

Run: `mise exec -- pnpm typecheck`

Expected: TypeScript exit code 0，无类型错误。

Run: `mise exec -- pnpm build`

Expected: `tsc -b` 与 Vite production build 均 exit code 0。

- [ ] **Step 4: 验证未变更的真实 Todo API 契约**

Run: `mise exec -- pnpm test:api tests/test_tasks_api.py -q`

Expected: `apps/api/tests/test_tasks_api.py` 全部 PASS，证明 Web 使用的五个服务端 operation 契约未漂移。

- [ ] **Step 5: 执行范围与差异检查**

Run: `rg -n "localStorage|sessionStorage|IndexedDB|createObjectURL|数据导出" apps/web/src/features/tasks apps/web/src/features/auth -g "!*.test.ts" -g "!*.test.tsx"`

Expected: 任务和认证生产代码无匹配；`rg` exit code 1 在此检查中代表预期的零匹配。ThemeProvider 的主题偏好持久化不属于任务或 token 持久化范围。

Run: `rg -n "TanStack|react-router" apps/web/src`

Expected: 无匹配，证明未引入全局请求库或路由器。

Run: `git diff --check`

Expected: exit code 0，无空白错误。

Run: `git status --short`

Expected: 只出现本阶段计划列出的文件；不得出现 `apps/api/data/*.db-wal`、`*.db-shm`、`.env`、缓存或构建产物。

- [ ] **Step 6: 人工响应式验收**

从仓库根目录分别运行 `mise exec -- pnpm dev:api` 和 `mise exec -- pnpm dev`，使用已有 CLI 账号登录。桌面验收右侧编辑面板，手机宽度验收底部抽屉；逐项验证快速新增、筛选、三种排序、加载更多、编辑保存/取消、脏表单确认、完成回滚提示、删除确认和账号时区。

Expected: 设计文档第 18 节全部验收项成立；人工验收不替代自动化命令。

- [ ] **Step 7: 提交文档校准**

```powershell
git add -- README.md AGENTS.md docs/roadmaps/2026-07-26-tickly-zero-to-one.md
git commit -m "docs: 标记 Todo Web 阶段完成"
```

## 实施约束

- 按 Task 1 到 Task 8 顺序执行；每个功能任务都先看到目标测试失败，再写最小实现并看到测试通过。
- 直接在当前 `main` 工作区实施，不创建 worktree，不调用子代理。
- 发现无关工作区改动时保留并绕开，不清理、不覆盖、不夹带提交。
- 本阶段只允许新增 `@internationalized/date`；任何其他依赖都先停止并重新确认范围。
- API 生产代码、migration、Docker、认证 Cookie 和 schema 不变，因此不扩大到 Docker smoke。
- 每次 commit 前精确核对 staged diff；阶段完成说明只列出实际运行且有 exit code 的验证。
